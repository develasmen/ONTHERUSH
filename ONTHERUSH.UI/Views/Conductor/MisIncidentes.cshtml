@model IEnumerable<ONTHERUSH.Abstracciones.DTOs.IncidenteDto>

@{
    Layout = null;
    ViewData["Title"] = "Mis incidentes";
}

<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Mis incidentes - ON THE RUSH</title>
    <link rel="stylesheet" href="~/css/pages/table-base.css" />
</head>
<body>
    <div class="table-page-container">

        <!-- Header -->
        <header class="table-header">
            <div class="table-header-content">
                <a href="@Url.Action("PanelConductor", "Conductor")" class="back-button">
                    <svg width="20" height="20" viewBox="0 0 20 20" fill="currentColor">
                        <path fill-rule="evenodd"
                              d="M9.707 16.707a1 1 0 01-1.414 0l-6-6a1 1 0 010-1.414l6-6a1 1 0 011.414 1.414L5.414 9H17a1 1 0 110 2H5.414l4.293 4.293a1 1 0 010 1.414z"
                              clip-rule="evenodd" />
                    </svg>
                    <span>Volver al panel</span>
                </a>

                <div class="table-header-info">
                    <h1 class="table-title">Mis incidentes</h1>
                    <p class="table-subtitle">
                        Consulte los incidentes reportados durante sus rutas
                    </p>
                </div>
            </div>
        </header>

        <!-- Main -->
        <main class="table-main">
            <div class="table-container">

                @if (Model == null || !Model.Any())
                {
                    <div class="empty-state">
                        <svg class="empty-icon" width="64" height="64" viewBox="0 0 20 20" fill="currentColor">
                            <path fill-rule="evenodd"
                                  d="M8.257 3.099c.765-1.36 2.722-1.36 3.486 0l6.518 11.591c.75 1.334-.213 2.99-1.742 2.99H3.48c-1.53 0-2.492-1.656-1.743-2.99L8.257 3.1zM11 13a1 1 0 10-2 0 1 1 0 002 0zm-1-6a1 1 0 00-1 1v3a1 1 0 102 0V8a1 1 0 00-1-1z"
                                  clip-rule="evenodd" />
                        </svg>
                        <h3>No ha reportado incidentes</h3>
                        <p>Cuando registre un incidente durante una ruta, aparecerá aquí.</p>
                    </div>
                }
                else
                {
                    <div class="table-wrapper">
                        <table class="data-table">
                            <thead>
                                <tr>
                                    <th>Fecha</th>
                                    <th>Tipo</th>
                                    <th>Descripción</th>
                                    <th class="text-center">Estado</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var incidente in Model)
                                {
                                    var pendiente = string.Equals(incidente.Estado, "Pendiente", StringComparison.OrdinalIgnoreCase);

                                    <tr>
                                        <td data-label="Fecha">
                                            <span class="date-text">@incidente.Fecha.ToString("dd/MM/yyyy")</span>
                                            <span class="time-text">@incidente.Fecha.ToString("HH:mm")</span>
                                        </td>

                                        <td data-label="Tipo">
                                            <span class="table-badge">@incidente.Tipo</span>
                                        </td>

                                        <td data-label="Descripción">
                                            <div class="value-new">@incidente.Descripcion</div>
                                        </td>

                                        <td data-label="Estado" class="text-center">
                                            @if (pendiente)
                                            {
                                                <span class="status-badge status-pending">
                                                    Pendiente
                                                </span>
                                            }
                                            else
                                            {
                                                <span class="status-badge status-completed">
                                                    @incidente.Estado
                                                </span>
                                            }
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                }

            </div>
        </main>

        <!-- Footer -->
        <footer class="table-footer">
            <p>© @DateTime.Now.Year ON THE RUSH · Conductor</p>
        </footer>
    </div>
</body>
</html>
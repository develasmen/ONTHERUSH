@{
    Layout = null;
}

<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Panel de Conductor - ON THE RUSH</title>

    <link rel="stylesheet" href="~/css/site.css" />
    <link rel="stylesheet" href="~/css/pages/conductor-dashboard.css" />
</head>
<body>
    <div class="dashboard-wrapper">

        <div class="dashboard-header">
            <div class="header-left">
                <h1>Panel de Conductor</h1>
                <p>Gestiona tu ruta asignada, actualiza estados y solicita cambios.</p>
            </div>
            <span class="role-badge">🚗 Rol: Conductor</span>
        </div>

        @if (TempData["MensajeBienvenidaConductor"] != null)
        {
            <div class="alert-soft">
                @TempData["MensajeBienvenidaConductor"]
            </div>
        }

        <div class="card-grid">

            <div class="dash-card">
                <div class="card-top">
                    <div>
                        <h3 class="card-title">Estado de viaje</h3>
                        <p class="card-desc">Actualiza el estado de los viajes asignados en tu ruta actual.</p>
                    </div>
                    <div class="card-icon">🟢</div>
                </div>

                <div class="card-actions">
                    <a class="btn-soft btn-primary-soft" href="@Url.Action("ActualizarEstadoViaje","Conductor")">
                        Actualizar estado →
                    </a>
                </div>
            </div>

            <div class="dash-card">
                <div class="card-top">
                    <div>
                        <h3 class="card-title">Solicitud de cambio de ruta</h3>
                        <p class="card-desc">Envía una solicitud para modificar el orden o paradas de la ruta.</p>
                    </div>
                    <div class="card-icon">🔁</div>
                </div>

                <div class="card-actions">
                    <a class="btn-soft btn-secondary-soft" href="@Url.Action("SolicitarActualizacionRuta","Conductor")">
                        Solicitar actualización →
                    </a>
                </div>
            </div>

            <div class="dash-card full">
                <div class="card-top">
                    <div>
                        <h3 class="card-title">Lista de pasajeros</h3>
                        <p class="card-desc">Visualiza las paradas asignadas y abre la dirección en Google Maps.</p>
                    </div>
                    <div class="card-icon">📍</div>
                </div>

                <div class="card-actions">
                    <a class="btn-soft btn-primary-soft" href="@Url.Action("VerPasajeros","Conductor")">
                        Ver pasajeros y direcciones →
                    </a>

                    <form asp-action="Logout" asp-controller="Auth" method="post" style="margin:0;">
                        <button type="submit" class="btn-soft btn-danger-soft">
                            Cerrar sesión
                        </button>
                    </form>
                </div>
            </div>

        </div>

        <div class="footer">
            <span>ON THE RUSH</span>
            <span>Conductor · Panel</span>
        </div>

    </div>
</body>
</html>